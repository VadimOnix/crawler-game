interface ProgressBarProps {
    /* пока не используются — шаблон боевой системы */
    color: string;
    percent: number;
}

const ProgressBar = (_props: ProgressBarProps) => {
    return (
        <div className="progressBar">

        </div >
    );
};

export default ProgressBar;
