// отображать текущую карту
let initialState = {};

const mapReducer = (state = initialState, action) => {
    switch (action) {
        default:
            return state;
    }
};

export default mapReducer;