// карта, если эксплоринг
// битва, если битва
let initialState = {};

const gamerReducer = (state = initialState, action) => {
    switch (action) {
        default:
            return state;
    }
};

export default gamerReducer;